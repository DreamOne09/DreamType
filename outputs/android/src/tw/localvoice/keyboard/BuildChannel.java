package tw.localvoice.keyboard;

/** Default is the GitHub APK; the bundle builder supplies a Play-specific copy. */
final class BuildChannel {
    static final boolean PLAY_STORE = false;
    private BuildChannel() {}
}
